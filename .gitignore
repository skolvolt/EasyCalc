node_modules/
web/dist/
data/quotemodel.db

# Local editor/tooling config (machine-specific)
.claude/

# Packaged app: track the hand-maintained launcher, config and version source,
# but ignore the heavy bundled runtime and the generated build outputs
# (regenerated by `npm run package`).
package-build/node/
package-build/app/node_modules/
package-build/app/server.mjs
package-build/web/

# Compiled installers
installer/Output/

# macOS build output (the assembled .app / zip)
mac-build/dist/
